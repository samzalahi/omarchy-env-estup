# Omarchy Environment Setup

This repository contains my personal automation scripts to quickly set up a fresh system. It installs essential apps, removes unnecessary bloatware/apps, and deploys custom dotfiles.

---

## Getting Started

To run the setup, navigate to the repository directory and execute the main script:

```bash
cd repos/omarchy-env-setup
chmod +x setup-env.sh
./setup-env.sh
```
